html
    title="Practica 4 - PUG"
    body
        h1(style={color: 'blue'})!=titulo
        span=min
        meter(
            id="disk_c"
            value=nivel 
            min=min
            max=max
        )
        span=max
